[package]
name = "bitboard"
version = "0.3.2"
edition = "2024"

[dependencies]
# External dependencies are allowed only when they are extremely
# high-performance and used in performance-critical paths.

