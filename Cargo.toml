[package]
name = "Cody"
version = "0.1.0"
edition = "2024"

[lib]
name = "cody"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(rust_analyzer)'] }

[dependencies]
crossbeam = "0.8.4"
once_cell = "1.21.3"
rand = "0.9.2"
regex = "1.11.2"

[build-dependencies]
regex = "1.11.2"

